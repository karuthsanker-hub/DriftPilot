# Brain Server dependencies (DGX Spark)
chromadb>=1.0.0
sentence-transformers>=3.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0

# Optional: pgvector backend (set BRAIN_DB_BACKEND=pgvector)
# Requires PostgreSQL with: CREATE EXTENSION vector;
psycopg[binary]>=3.2.0
pgvector>=0.3.0
